spec:
  name: avatars-api
  services:
    - environment_slug: node-js
      git:
        branch: main
        repo_clone_url: https://github.com/DiceBear/avatars-api.git
      http_port: 3000
      name: avatars-api
      routes:
        - path: /
      run_command: yarn start
